.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.superpowers/
.DS_Store

# Local round history, raw vendor exports, GPS/course caches, and generated maps.
# These contain personal playing/location data and are not needed to run the code.
/data/
/app/dist/
/app/*.tgz

# Keep the lockfile in the project so other devices reproduce the environment.

# Credentials, signing material, and local tooling state (all subprojects).
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.mobileprovision
*.keystore
*.jks
*.xcuserstate
xcuserdata/
DerivedData/
node_modules/
test-results/
playwright-report/
*.db
*.db-*
*.sqlite
*.sqlite3
*.sqlite3-*
*.xcarchive
*.ipa
/dist/
