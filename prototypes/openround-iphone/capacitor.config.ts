import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.marcusgoll.openround',
  appName: 'OpenRound',
  webDir: 'dist/client',
};
export default config;
