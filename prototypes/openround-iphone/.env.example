# Optional public browser key. Empty selects the no-key imagery provider.
# VITE_ values are embedded in the built app; never put secrets here.
VITE_GOOGLE_MAPS_API_KEY=
# Production uses bundled course geometry by default.
VITE_OPENROUND_ALLOW_LIVE_OVERPASS=false
